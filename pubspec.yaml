name: alphabet_learning_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^5.2.1
  cupertino_icons: ^1.0.8
  google_fonts: ^4.0.3
  flame: ^1.8.1
  provider: ^6.1.2
  animate_do: ^3.0.2
  flutter_animate: ^4.5.2
  glassmorphism: ^3.0.0
  path_drawing: ^0.5.1+1
  flutter_colorpicker: ^1.1.0
  # flutter_svg: ^0.22.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # fonts:
  #   - family: Comic Sans MS
  #     fonts:
  #       - asset: assets/fonts/ComicSansMS.ttf

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/audio/
    - assets/images/
    - assets/avatars/
    - assets/images/coloring_book.png